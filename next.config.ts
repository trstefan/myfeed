// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["img.lemde.fr", "ichef.bbci.co.uk"],
  },
};

module.exports = nextConfig;
